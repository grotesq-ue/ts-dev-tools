pre-commit:
  jobs:
    - run: bun x tsc --noEmit
    - run: bun x oxlint --fix {staged_files}
      stage_fixed: true
    - run: bun x dprint fmt --allow-no-files {staged_files}
      stage_fixed: true
    - run: bun test
